1、计算属性 - computed
    类似于data，唯一不同的是这个属性可以计算
属性 必须是函数
c:function(){
    return 计算输出值
}

computed:{
    c:{
        //普通的读取数据时调用到的函数
        get:function(){

        },
        //计算数据的代码行写这里
        set:function(参数){

        }
    }
}

$watch() <= 看-盯着 一但监控的对象发生改变就会有个回调函数
帮我们执行，只有当数据真正发生改变才会发生改变，如果数据
内容没变的话是不算的。